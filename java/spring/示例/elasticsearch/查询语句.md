# 映射接口

```java
import org.springframework.cache.annotation.Cacheable;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.elasticsearch.annotations.Query;
import org.springframework.data.elasticsearch.repository.ElasticsearchRepository;

public interface BookPageRepository extends ElasticsearchRepository<BookPageDocument, String> {
}
```

# 单字段匹配（content为字段名称）

```java
@Query("""
    {
      "bool":{
        "must":[
          {
            "match_phrase":{
              "content":{
                "query":"?0"
              }
            }
          }
        ]
      }
    }
    """)
```

# 多字段匹配（只需要匹配任意一个字段）

```
@Query("""
       {
          "bool": {
             "should": [{
                "query_string": {
                   "query": "?0",
                   "fields": ["name"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["whole_address"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["remark"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["start_time_remark"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["end_time_remark"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["pitch_row"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["owner"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["exploration"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["exploration_team"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["stage"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["owner_name"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["owner_phone_number"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["exploration_name"]
                }
             }, {
                "query_string": {
                   "query": "?0",
                   "fields": ["exploration_phone_number"]
                }
             }]
          }
       }
    """)
```