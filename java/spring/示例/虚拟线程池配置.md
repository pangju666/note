```java
@SpringBootConfiguration
public class BeanConfig {
    @Bean("virtual-task-executor")
    public AsyncTaskExecutor asyncTaskExecutor() {
       return new TaskExecutorAdapter(Executors.newVirtualThreadPerTaskExecutor());
    }
}
```