文件名：application-test.yml

文件位置：src/main/resources

```yaml
spring:
  # quartz配置，建议只在生产或测试环境开启定时器
  quartz:
    properties:
      org:
        quartz:
          scheduler:
            wrapJobExecutionInUserTransaction: false
          threadPool:
            class: org.quartz.simpl.SimpleThreadPool
            threadsInheritContextClassLoaderOfInitializingThread: true
            threadCount: 10
            threadPriority: 5
          jobStore:
            useProperties: false
            tablePrefix: QRTZ_
            isClustered: false
            class: org.quartz.impl.jdbcjobstore.JobStoreTX
            driverDelegateClass: org.quartz.impl.jdbcjobstore.StdJDBCDelegate
            dataSource: quartzDS
          dataSource:
            quartzDS:
              driver: com.mysql.cj.jdbc.Driver
              # quartz信息所在数据库地址
              URL: jdbc:mysql://mysql:3306/quartz?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
              username: root
              password: gTz_2021
              # 连接池类型
              provider: hikaricp
              # 连接池最大生命周期，需要根据数据库配置修改
              maxLifetime: 14400000
```