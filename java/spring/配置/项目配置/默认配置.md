文件名：application.yml

文件位置：src/main/resources

```yaml
server:  
  servlet:  
    context-path: / # 上下文路径  
    encoding:  
      force: true # 强制编码请求和响应（默认使用utf-8，防止乱码）  
  port: 80   # 端口号设置  
  tomcat:  
    max-connections: 10000 # 最大连接数  
    threads:  
      max: 800 # 最大工作线程数  
      min-spare: 100 # 最小空闲线程数  
    accept-count: 100 # 等待队列长度  
    connection-timeout: 20000

spring:
  # 禁用 banner  
  main:
    banner-mode: off
  # 开启虚拟线程池  
  threads:
    virtual:
      enabled: true
  task:
    execution:
      pool:
        core-size: 8 # cpu密集型设置cpu核心数，io密集型则设置为cpu核心数的2-3倍
        max-size: 16
        queue-capacity: 100 
        keep-alive: 60s 
      thread-name-prefix: async-task- 
    scheduling: 
      pool: 
        size: 4 
      thread-name-prefix: scheduling-
  servlet:
    multipart:
      max-file-size: 5GB # 最大上传文件大小    
      max-request-size: 5130MB # 最大请求体大小（建议比最大上传文件大小大10MB)
      file-size-threshold: 2KB
      resolve-lazily: false
  # jackson时区和时间格式化设置
  jackson:
    # 设置时区  
    time-zone: GMT+8  
    # 设置时间格式  
    date-format: yyyy-MM-dd HH:mm:ss  
    # 设置语言  
    locale: CHINESE  
    # 序列化策略
    generator:  
      # 浮点数过大时防止返回科学计数  
      write-bigdecimal-as-plain: true
    mapper:  
	  # 不区分枚举名称大小写  
	  accept-case-insensitive-enums: true
  cache:
    # 缓存类型为redis  
    type: redis
    redis:
      # 允许缓存null值  
      cache-null-values: true
  # SQL数据源配置    
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/public_file?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: xxxx
    # 连接池最大生命周期，需要根据数据库配置修改
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000     
      leak-detection-threshold: 60000
  data:
    # redis连接配置  
    redis:
      host: redis
      password: xxxx
      database: 6
      # lettuce连接池配置，建议不要修改
      lettuce:
        cluster:
          refresh:
            adaptive: true
            period: 20
    # mongodb连接配置  
    mongodb:
      host: mongodb.default
      database: public_service_file
      username: root
      password: xxxxx
      # 认证数据库，存在用户名密码时必填
      authentication-database: admin
  # elasticsearch配置
  elasticsearch:
    uris: http://172.16.0.120:30003
    username: elastic
    password: T0s0LyHNbd7MfUm
  # kafka配置    
  kafka:
    # 服务url  
    bootstrap-servers: kafka.default:9092
    # 生产者配置，建议不要修改
    producer:
      retries: 1
      batch-size: 16384
      buffer-memory: 1024000
      acks: 1
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring:
          json:
            trusted:
              packages: "*"
    # 消费者配置
    consumer:
      # 一般设置为项目名称即可
      group-id: public-service-file
      # 其他配置不要修改
      enable-auto-commit: false
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: "*"
    # 监听器配置，建议不要修改
    listener:
      ack-mode: manual_immediate
  # quartz 定时器配置
  quartz:
    # 将定时器信息存储在数据库中  
    job-store-type: JDBC
    # 不执行初始化sql脚本，需要手动初始化数据库
    jdbc:
      initialize-schema: NEVER

# mybatis-plus 配置
mybatis-plus:
  # 实体类所在包名
  type-aliases-package: cn.changtech.publicservice.file.model.entity
  # mapper文件路径
  mapper-locations: classpath:mapper/*.xml
  # 从sql中删除额外的空格字符
  configuration:
    shrink-whitespaces-in-sql: true
  global-config:
    # 禁用banner  
    banner: false
    db-config:
      # 列名使用``包裹  
      column-format: "`%s`"
      # 更新时忽略null值
      update-strategy: always
	  # 插入时忽略null值
	  insert-strategy: always
      # id类型默认为自增id
      id-type: auto
  # 插件配置    
  plugins:
    # 开启防止全表更新与删除插件  
    block-attack:
      enabled: true
    # 开启乐观锁插件  
    optimistic-locker:
      enabled: true

logging:
  file: name: app.log 
  logback: 
    rollingpolicy: 
      max-file-size: 100MB 
      max-history: 30 
      total-size-cap: 3GB
```